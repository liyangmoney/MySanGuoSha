# 三国杀游戏后端

此文件夹包含基于 Python 的 Flask 框架开发的后端服务，以支持三国杀游戏。

## 快速开始

### 先决条件
- Python 3.8+
- pip 包管理器

### 安装依赖
```bash
pip install -r requirements.txt
```

### 运行服务
```bash
python app.py
```

服务默认运行在 `http://127.0.0.1:5000/`。

## 文件结构
- `app.py`: 主后端逻辑。
- `requirements.txt`: Python 依赖。
